import AxeDevToolsLogger from '@axe-devtools/logger';
declare class Reporter extends AxeDevToolsLogger {
    /**
     * @param reportName Name of report
     * @param directory Directory containing AxeDevTools output JSON files
     */
    constructor(reportName: string, directory?: string);
    /**
     * Build an HTML report and save it to the given `directory`.
     *
     * @param directory Output directory
     * @param userAgent
     */
    buildHTML(directory: string, userAgent?: string): Promise<void>;
    /**
     * Build an XML report and save it to the given `directory`.
     *
     * @param directory Output directory
     * @param userAgent
     */
    buildJUnitXML(directory: string, userAgent?: string): Promise<void>;
    /**
     * Build a CSV report and save it to the given `directory`
     *
     * @param directory
     * @param userAgent
     * @param filterOpts - array of result type to filter before building the CSV
     */
    buildCSV(directory: string, userAgent?: string, filterOpts?: string[]): Promise<void>;
    private makeFileName;
}
export default Reporter;
